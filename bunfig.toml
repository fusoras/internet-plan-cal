[install]
# Only install package versions published at least 2 days ago
minimumReleaseAge = 172800 # seconds

# Exclude trusted packages from the age gate
minimumReleaseAgeExcludes = ["@types/node", "typescript"]
