Is necesary bun to run the project, you can install it from https://bun.sh/

In the root of the android project, run:
```bash
bun run dev:android
```
In the root of the console project (TypeScript, bun), run:
```bash
bun run dev:console
```